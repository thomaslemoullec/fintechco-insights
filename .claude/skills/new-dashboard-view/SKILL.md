---
name: new-dashboard-view
description: Load the reuse map before planning or building a new dashboard view / UI capability for the Macro Insights service (existing indicators, analysis/pairing logic, chart helpers, disclosure component). Use when asked to plan or implement a new tab, view, or chart in services/insights.
---

# new-dashboard-view

Read `services/insights/implementation_guidelines.md` first — it documents what already
exists (indicators, analysis functions, chart/component helpers) for an additive dashboard
view, so planning starts from there instead of re-deriving it. Also check
`services/insights/web/DESIGN.md` for the design system a new view must follow.

Open a specific file named in the guidelines directly when you need to confirm its exact
current syntax to write against it.
